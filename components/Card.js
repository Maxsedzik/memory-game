import { View, Text, StyleSheet } from "react-native";

const Card = () => {
	return (
		<View>
			<Text>Memory Game</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default Card;
